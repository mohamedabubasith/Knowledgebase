'use client';import {FormEvent,useEffect,useState} from 'react';import {api} from '@/lib/api';export default function Users(){const [items,setItems]=useState<any[]>([]);const load=()=>api('users').then(setItems);useEffect(()=>{load()},[]);async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await api('users',{method:'POST',body:JSON.stringify({email:f.get('email'),password:f.get('password'),full_name:f.get('full_name'),role:f.get('role')})});e.currentTarget.reset();load()}return <><h1 className="page-title">Users</h1><p className="muted">Control platform access and roles.</p><form className="card" onSubmit={create} style={{display:'grid',gridTemplateColumns:'repeat(5,1fr)',gap:12,padding:16,margin:'22px 0'}}><input className="input" name="full_name" placeholder="Full name"/><input className="input" name="email" type="email" required placeholder="Email"/><input className="input" name="password" type="password" required placeholder="Password"/><select className="input" name="role"><option>user</option><option>admin</option><option>super_admin</option></select><button className="button">Create</button></form><div className="card"><table className="table"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Active</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.full_name}</td><td>{x.email}</td><td><span className="badge">{x.role}</span></td><td>{x.is_active?'Yes':'No'}</td></tr>)}</tbody></table></div></>}
