'use client';import {FormEvent,useEffect,useState} from 'react';import {api} from '@/lib/api';export default function KnowledgeBases(){const [items,setItems]=useState<any[]>([]);const load=()=>api('knowledge-bases').then(setItems);useEffect(load,[]);async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await api('knowledge-bases',{method:'POST',body:JSON.stringify({name:f.get('name'),description:f.get('description')})});e.currentTarget.reset();load()}return <><h1 className="page-title">Knowledge bases</h1><p className="muted">Organize documents into secure retrieval spaces.</p><form className="card" onSubmit={create} style={{display:'flex',gap:12,padding:16,margin:'22px 0'}}><input className="input" name="name" required placeholder="Knowledge base name"/><input className="input" name="description" placeholder="Description"/><button className="button">Create</button></form><div className="grid-cards">{items.map(x=><div className="card" key={x.id} style={{padding:20}}><h3>{x.name}</h3><p className="muted">{x.description||'No description'}</p><div className="badge">{x.is_public?'Public':'Private'}</div></div>)}</div></>}