'use client';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';export default function Login(){const router=useRouter();const [error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const r=await fetch('/frontend-api/session/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:form.get('email'),password:form.get('password')})});if(r.ok)router.push('/dashboard');else setError('Invalid email or password')}return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',background:'radial-gradient(circle at 20% 20%,#2a2055,transparent 40%)'}}><form className="card" onSubmit={submit} style={{width:380,padding:32}}><div style={{fontSize:27,fontWeight:900}}>◈ ATLAS <span style={{color:'#8d70ff'}}>KB</span></div><p className="muted">Sign in to your private knowledge platform.</p><label>Email<input className="input" name="email" type="email" required style={{margin:'7px 0 16px'}}/></label><label>Password<input className="input" name="password" type="password" required style={{margin:'7px 0 20px'}}/></label>{error&&<p style={{color:'#ff6577'}}>{error}</p>}<button className="button" style={{width:'100%'}}>Sign in</button></form></main>}